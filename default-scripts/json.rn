// Collect system state and store it as structured JSON.
// Usage:
//   state-collector json

pub async fn collect() {
    sc::log(`starting collection at ${sc::timestamp()}`);
    sc::write("state.json", sc::serialize_snapshot(sc::snapshot()?, Format::PrettyJson)?)?;
    sc::log("JSON collection complete.");
}
